# 2026-05-18 Log

- Read `docs/readme.md`, `docs/plan and goal.md`, the per-setting random-walk design/management documents, and current summary/todo files.
- Identified redundant design-only markdown files covering random-walk Setting I, Setting II, Setting III, the supplementary no-shared setting, the design check, and generated true-estimate summary.
- Consolidated design goals, sample sizes, parameter targets, and calibrated true-value summaries into root-level `RANDOM_WALK_SETTING_DESIGN_SUMMARY.md`.
- Updated active control references so future agents start from the consolidated root design summary.
- Updated `Simulation_random_walk/summarize_true_estimates.R` so it no longer recreates the deleted standalone true-estimate markdown file.
- Initial push attempt to `git@github.com:Xuezhixing-Zhang/SharedQ.git` failed with `ssh: connect to host github.com port 22: Connection timed out`; remote will be switched to SSH over port 443 before retrying.
- Revised `RANDOM_WALK_SETTING_DESIGN_SUMMARY.md` to distinguish truly shared or near-shared design parameters from candidate-only shared groups and to spell out the `mu`/`sigma` relationships.
- Added the executable Setting III staged primary-outcome equations to `RANDOM_WALK_SETTING_DESIGN_SUMMARY.md` and noted that the supplementary no-shared setting reuses them.
- Added a calibration objective and constraints summary explaining the COBYLA search, unbounded raw `gamma`, and the projected Q-coefficient inequality windows used for calibrated true-value generation.
- Implemented target-centered candidate constraints in all four random-walk calibration scripts and added `Simulation_random_walk/validate_candidate_calibration.R` for coefficient and implied-difference pass/fail reports.
- Verified the updated calibration and validation scripts parse under R 4.4.0.
- Verified each setting's inequality-constraint vector length matches the new candidate constraint count; updated the continuous-setting Jacobian helper to use the same count.
- Ran `Rscript Simulation_random_walk/validate_candidate_calibration.R`; the current pre-recalibration artifacts fail the new `0.03` candidate tolerance gate, and the generated `candidate_calibration_report.md`/`.csv` files record the required recalibration gaps.
- Added `Simulation_random_walk/run_candidate_calibration_smoke.R` so bounded constrained calibration checks run into ignored `calibration/candidate_constraint_smoke/` directories without overwriting production artifacts.
- Ran `Rscript Simulation_random_walk/run_candidate_calibration_smoke.R` with `mc_n = 5000`, `maxeval = 50`, `local_maxeval = 20`, and `n_starts = 1` for all settings/specs. All setting-level smoke reports still fail the `0.03` gate, but Setting II `separated_moderate`, many Setting III terms, and the supplementary moderate spec are near the target windows; production calibration is blocked until smoke failures are reviewed.
