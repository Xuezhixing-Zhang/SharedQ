Please read the markdown files for algorithm, simulation settings and workflows (they are under each setting folder). For setting 2 and 3, we have not updated them yet, so you will need to complete and update these files by yourself. 

The current things you need to do:

1. First, review the markdown files in the folders to know what you should do.
2. For each setting, understand what we are going to do (the workflow), then propose several parameter group choices, use nloptr to find true estimates at the population level, then run the simulations for the sample sizes mentioned in the markdown file. Note that running simulations require you to write pbs file to submit jobs to the cluster.
3. For each setting, use folders to classify our files, test codes, test results, working log, simulation results, evaluation results. Don't make it messy.
4. First do setting I, after you believe you have understood the workflow well, do setting II and III.
5. Do remember to write key points and update workflows when necessary. This is for other agents to review.
6. For the files you created, do create a file (txt or markdown) to describe what these files are used to do.