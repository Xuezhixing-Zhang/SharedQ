# 2026-05-21 Log

- Resumed RW-CAL work from `docs/readme.md`, `docs/plan and goal.md`, `RANDOM_WALK_SETTING_DESIGN_SUMMARY.md`, and current per-setting summaries.
- Current scheduler status: `qstat -u e1404425` shows no active jobs for this user. The prior Setting II, Setting III, and Supplementary Setting III No Shared calibration jobs are no longer running.
- Current artifact status before new work: production validation remains blocked. Setting II default production calibration hit the 12-hour PBS walltime and did not update `calibration_separated_moderate.rds`; Setting I default still fails materially; Setting III and Supplementary Setting III No Shared default artifacts have boundary-level validation misses.
- Implemented `VALIDATION_SPEC_MODE=default`, `VALIDATION_SPECS`, and optional validation report stem/title controls in `Simulation_random_walk/validate_candidate_calibration.R`.
- Implemented `CALIBRATION_TARGET_TOLERANCE` support in `Simulation_random_walk/calibration_runner_utils.R` so production calibration jobs can use stricter internal optimizer windows without relaxing acceptance thresholds.
- Updated production simulation script defaults to read setting-specific default calibration artifacts rather than stale `alternative_pars.rds` files.
- Updated Setting III and Supplementary Setting III No Shared calibration PBS scripts to use absolute log paths.
- Updated control docs to record the default-only validation gate, stricter internal calibration-window control, and current calibration blockers.
- Syntax check parsed 11 changed R scripts successfully under `module load r/4.4.0`.
- Ran `VALIDATION_SPEC_MODE=default Rscript Simulation_random_walk/validate_candidate_calibration.R`; all four current default artifacts still fail, as expected before rerunning constrained calibration. New default reports were written under each setting's `Summarize/candidate_calibration_default_report.*`.
- Submitted default production calibration retries on queue `long`, walltime `72:00:00`, `CALIBRATION_MC_N=1000000`, `CALIBRATION_MAXEVAL=100000`, `CALIBRATION_LOCAL_MAXEVAL=20000`, `CALIBRATION_N_STARTS=3`, and `CALIBRATION_SPEC_MODE=default`.
  - Setting I `balanced_small`, internal target tolerance `0.009`: `507655.hn-10-03`
  - Setting II `separated_moderate`, internal target tolerance `0.029`: `507656.hn-10-03`
  - Setting III `rw_sigma_moderate`, internal target tolerance `0.009`: `507657.hn-10-03`
  - Supplementary Setting III No Shared `separated_moderate`, internal target tolerance `0.029`: `507658.hn-10-03`
- `qstat -u e1404425` showed all four submitted calibration jobs in `R` state on queue `long` immediately after submission.
- User requested a checkpoint and stop. Deleted jobs `507655.hn-10-03`, `507656.hn-10-03`, `507657.hn-10-03`, and `507658.hn-10-03` with `qdel`.
- Checkpoint status after deletion: `qstat -u e1404425` returned no active jobs. Calibration logs show the jobs started with the intended default specs and controls, but no new calibration `.rds` artifacts were written before deletion.
