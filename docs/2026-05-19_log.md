# 2026-05-19 Log

- Started RW-CAL implementation from the approved plan.
- Pre-existing untracked paths observed and left untouched: `.codex`, `DataAnalysis_I_exact_same/`, `Simulation_exact_same/`.
- Scope: tighten production candidate target windows for true near-shared random-walk settings only, add configurable calibration runners, and preserve separated targets for no-shared settings.
- Added shared calibration runner env-var helpers and patched smoke/spec calibration runners to support `CALIBRATION_SPEC_MODE`, `CALIBRATION_SPECS`, `CALIBRATION_MC_N`, `CALIBRATION_MAXEVAL`, `CALIBRATION_LOCAL_MAXEVAL`, `CALIBRATION_N_STARTS`, and `CALIBRATION_PRINT_LEVEL`.
- Set Setting I and Setting III candidate target tolerances to `0.01`; left Setting II and Supplementary Setting III No Shared at `0.03`.
- Updated validation reporting to use per-setting tolerances and added a Setting II specs PBS entrypoint.
- `Rscript` was not available on the default shell `PATH`; `module load r/4.4.0 && Rscript ...` parsed 9 changed R files successfully.
- Updated control/design/management/file-manifest docs to record the strict shared-setting tolerance policy, configurable calibration runner env vars, and the new Setting II specs PBS entrypoint.
- Ran production artifact validation with per-setting tolerances; all four settings remain `FAIL`, as expected for stale pre-recalibration `.rds` artifacts.
- Ran default-spec smoke calibration with `CALIBRATION_MC_N=5000`, `CALIBRATION_MAXEVAL=1000`, `CALIBRATION_LOCAL_MAXEVAL=500`, and `CALIBRATION_N_STARTS=5`. Initial reports failed all defaults; after adding numerical slack for exact-boundary comparisons and regenerating reports in report-only mode, Setting II `separated_moderate`, Setting III `rw_sigma_moderate`, and Supplementary Setting III No Shared `separated_moderate` pass. Setting I `balanced_small` still fails materially.
- Ran a higher-budget Setting I default smoke retry with `mc_n=10000`, `maxeval=5000`, `local_maxeval=2000`, and `n_starts=10`. The retry completed with COBYLA unused-gradient warnings, but Setting I still fails the `0.01` gate: `Q3_A1`, `Q2_A2`, `Q3_A1A3`, and `Q2_A1A2` remain outside tolerance.
- Submitted production default calibration jobs for defaults whose smoke reports pass:
  - Setting II `separated_moderate`: `504736.hn-10-03`
  - Setting III `rw_sigma_moderate`: `504737.hn-10-03`
  - Supplementary Setting III No Shared `separated_moderate`: `504738.hn-10-03`
- `qstat` showed all three submitted production calibration jobs in `R` state on queue `short` immediately after submission.
- Final syntax check parsed 11 changed R scripts successfully under `module load r/4.4.0`.
- Follow-up `qstat` still showed production calibration jobs `504736.hn-10-03`, `504737.hn-10-03`, and `504738.hn-10-03` in `R` state.
- Verified current report statuses: default smoke is `PASS` for Setting II, Setting III, and Supplementary Setting III No Shared, `FAIL` for Setting I; production artifact validation remains `FAIL` for all settings until the submitted production calibration jobs complete and Setting I is resolved.
- Latest `qstat` check showed all three production calibration jobs still running with about `00:01:32` CPU time each.
- Before commit/push, `qstat` showed jobs `504736.hn-10-03`, `504737.hn-10-03`, and `504738.hn-10-03` still in `R` state with about `00:21:41` to `00:21:43` CPU time. Production validation and simulation reruns remain blocked until these jobs complete and Setting I is resolved.
- After pushing commit `0589da1`, a final `qstat` check showed the same three jobs still in `R` state with about `00:23:41` to `00:23:43` CPU time.
