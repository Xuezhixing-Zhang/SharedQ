# 2026-05-13 Work Log

## Document Management Rebuild

- Read agent instructions, Setting I handoff/workflow/manifest/design documents, and the existing document inventory.
- Confirmed the workspace `.git` directory is empty, read-only, and not recognized by Git.
- Initialized external Git metadata at `/tmp/SharedQ.git` with work tree `/data/cheungyb/home/e1404425/SharedQ`.
- Configured remote `origin` as `git@github.com:Xuezhixing-Zhang/SharedQ.git`.
- Verified through the GitHub connector that `Xuezhixing-Zhang/SharedQ` exists, is empty, and allows push/admin access.
- Attempted an SSH remote check from the shell with noninteractive SSH options; it timed out on `github.com:22`, so normal `git push` may be blocked by environment network or SSH configuration.
- Started Milestone 2 by creating the document control files in `docs/`.
- Completed Milestone 2 content updates. Next action is local commit and push attempt for the control files.
- Committed Milestone 2 locally as `f038ec9` with message `Add document control files`.
- Push attempt to `git@github.com:Xuezhixing-Zhang/SharedQ.git` failed because SSH to `github.com:22` timed out.
- Retry through `ssh://git@ssh.github.com:443/Xuezhixing-Zhang/SharedQ.git` reached GitHub but failed with `Permission to Xuezhixing-Zhang/SharedQ.git denied to deploy key`.
- Explained access-right options: add the correct user SSH key, add a write-enabled deploy key to the repo, or use HTTPS with a token.
- Completed Milestone 3 by moving all discovered document files into top-level `docs/` using source-prefixed names.
- Verified no markdown, `.docx`, readme, or `.txt` documents remain outside `docs/`.
- Committed Milestone 3 locally as `2d384b4` with message `Move documents into docs hub`.
- Push attempt for Milestone 3 through `ssh.github.com:443` failed with `Permission to Xuezhixing-Zhang/SharedQ.git denied to deploy key`.
- Generated a new persistent deploy key pair for this repository at `/data/cheungyb/home/e1404425/.ssh/sharedq_deploy_key`.
- Public deploy key provided to the user for GitHub setup with write access:
  `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAOVjK9mgdlLK7nQ3vYzhboRoIXuOwbZm7xrxurOer+U sharedq-deploy-key-2026-05-13`
- Configured local Git metadata to use `/data/cheungyb/home/e1404425/.ssh/sharedq_deploy_key` for future SSH push attempts.
- Completed Milestone 4 by updating active document references and changing `Simulation_random_walk/summarize_true_estimates.R` to write `docs/simulation_random_walk_true_estimates_sigma_summary.md`.
- Verified the only remaining old document paths are mapping examples in `docs/readme.md`.
- Committed Milestone 4 locally as `7b7e8af` with message `Update document references`.
- Push attempt for Milestone 4 with the new key failed with `git@ssh.github.com: Permission denied (publickey)`, which is expected until the public key is added to the GitHub repository.
- Ran document-layout validation: no markdown, `.docx`, readme, or `.txt` documents remain outside `docs/`.
- Ran stale-reference search: only intentional path-mapping examples remain in `docs/readme.md`.
- Ran `module load cmake/3.21.4 r/4.4.0 && Rscript Simulation_random_walk/summarize_true_estimates.R`; it completed successfully and wrote the summary into `docs/`.
- Git status for tracked document-management changes is clean; untracked simulation/code/data directories remain because this session had to initialize external Git metadata from an empty local history.
- Completed Milestone 5 final validation.
- Committed Milestone 5 locally as `8e57d28` with message `Record document migration validation`.
- Final push through `ssh://git@ssh.github.com:443/Xuezhixing-Zhang/SharedQ.git` succeeded and set local `main` to track the remote branch.
- Started random-walk per-setting file-management rebuild.
- Inspected current simulation artifacts and found large generated result files, including `Simulation_random_walk/Setting3/results_1000.rds` at about 267 MB. These generated binaries will be reorganized locally but ignored in normal Git commits unless Git LFS is introduced.
- Added per-setting management docs covering code-file usage, simulation design, parameter choices, seeds, current result availability, and target artifact structure.
- Committed and pushed RW-FM 1 as `75d5ba1`.
- Moved generated `.rds` and `.out` artifacts into setting-local `calibration/`, `simulation_results/`, `test_results/`, and `logs/` folders.
- Patched simulation, calibration, test, evaluation, PBS, and true-estimate summary paths to use the categorized folders.
- Added `.gitignore` rules so large generated binaries remain local and normal Git commits contain reproducible code/docs/folder structure.
- Committed and pushed RW-FM 2 as `829e9de`.
- Added `Simulation_random_walk/summarize_setting_results.R` and generated each setting's `Summarize/current_results_summary.md`, `Summarize/evaluation_summary.csv`, and `Summarize/todo.md`.
- Current summary output confirms Settings I, II, and III have production results for `n = 100, 300, 500, 1000` with `200` non-null replicates each; Supplementary Setting III No Shared has no production `results_*.rds` files yet.
- Committed and pushed RW-FM 3 as `d305db9`.
- Ran quick smoke tests for Setting I, Setting II, Setting III, and Supplementary Setting III No Shared after artifact relocation; all completed successfully. R emitted the known `timedatectl` bus warning, and Setting I/II emitted small-sample optimization warnings during bounded checks.
- Found that the first test cleanup guard used top-level `on.exit()`, which did not restore production `calibration/data_original.rds` after quick tests.
- Patched all four `test.R` files to restore pre-existing `calibration/data_original.rds` immediately after the bounded `Q_learning_Setting_*()` population-generation call with `tryCatch(..., finally = restore_population_file())`.
- Regenerated production `calibration/data_original.rds` for Settings I, II, and III from their production `alternative_pars.rds` files using `mc_n = 1000000`.
- Re-ran all four quick smoke tests after the cleanup patch; all passed.
- Verified post-test population file sizes stayed at production scale for Settings I, II, and III: Setting I `2992618` bytes, Setting II `2975867` bytes, Setting III `55532821` bytes. Supplementary Setting III No Shared remains a test-scale local artifact at `92649` bytes because no production result set exists yet.
- Re-ran `Simulation_random_walk/summarize_true_estimates.R` and `Simulation_random_walk/summarize_setting_results.R`; both completed successfully.
- Validated that no generated `.rds` or `.out` files remain at `Simulation_random_walk` depth 2; generated artifacts are now under setting-local categorized folders.
- Searched R code for old shallow setting-path reads/writes; remaining references point to `calibration/`, `simulation_results/`, or `test_results/`.
- Completed RW-FM 4 and prepared RW-FM 5 final commit and push.
- Committed and pushed the RW-FM 4/RW-FM 5 validation checkpoint as `5991abf`.
