# 2026-05-27 Log

- Checked active Codex goal state: no thread-level goal was set at the start of the session. Set the active goal to complete the random-walk calibration gate workflow, confirm smoke/gate prerequisites, promote selected candidate parameter values, validate all default calibrations, start full simulations, and report scheduler/workspace status.
- Verified external Git metadata is still required because the workspace `.git` directory is not a normal writable checkout. Use `git --git-dir=/tmp/SharedQ.git --work-tree=/data/cheungyb/home/e1404425/SharedQ ...` for Git operations.
- Confirmed `qstat -u e1404425` initially showed no active jobs, meaning the 2026-05-24 calibration and gate-candidate jobs had finished.
- Ran `Rscript Simulation_random_walk/select_calibration_gate_candidate.R`; all four default settings reported `PASS`.
- Promoted selected gate candidates with `CALIBRATION_GATE_PROMOTE=1 Rscript Simulation_random_walk/select_calibration_gate_candidate.R`.
  - Setting I `balanced_small`: selected `gate_candidates/tol006`, max absolute validation error about `0.00685`.
  - Setting II `separated_moderate`: selected `gate_candidates/tol024`, max absolute validation error `0.024`.
  - Setting III `rw_sigma_moderate`: selected `gate_candidates/tol006`, max absolute validation error `0.006`.
  - Supplementary Setting III No Shared `separated_moderate`: selected `gate_candidates/tol024`, max absolute validation error `0.024`.
- Ran `VALIDATION_SPEC_MODE=default Rscript Simulation_random_walk/validate_candidate_calibration.R`; Setting I, Setting II, Setting III, and Supplementary Setting III No Shared all passed the default validation gate.
- Confirmed the May 19 Setting I calibration smoke report is stale relative to the promoted May 27 production artifact. The fresh selector and default validation reports are the active gate for production.
- Submitted full production simulations after the default gate passed:
  - Setting I: `516131.hn-10-03`
  - Setting II: `516132.hn-10-03`
  - Setting III: `516133.hn-10-03`
  - Supplementary Setting III No Shared: `516134.hn-10-03`
- `qstat -u e1404425` showed all four production jobs running on queue `short` with requested walltime `48:00`.
- Added `Simulation_random_walk/make_result_tables.R` to build manuscript-style tables after production completion. The script follows the existing `Writing/Tables and Figures.docx` and `Writing/Suppl Tables and Figures.docx` structure, checks that all result files are complete and newer than the promoted calibration artifact, writes main/supplemental tables under `Simulation_random_walk/Writing/generated_tables/`, and produces a method-claim comparison of SQ learning against Q learning and Q shared.
- Ran `TABLE_STATUS_ONLY=1 Rscript Simulation_random_walk/make_result_tables.R`. It correctly refused final table generation because production outputs are still incomplete/currently running: current May 27 result files exist only for `n=100`, while older `n=300`, `n=500`, and `n=1000` files are stale relative to the May 27 promoted calibration artifacts.
- Pre-push checkpoint at 2026-05-27 15:17 +08: all four full simulation jobs were still running. Refreshed `Simulation_random_walk/Writing/generated_tables/production_result_status.*`; current `n=100` non-null replicate counts were Setting I `54`, Setting II `42`, Setting III `66`, and Supplementary Setting III No Shared `110`, while later sample sizes were not yet refreshed under the promoted calibration.
- Next checkpoint: monitor those four jobs; after completion, run `Rscript Simulation_random_walk/summarize_setting_results.R`, then `Rscript Simulation_random_walk/make_result_tables.R`, inspect production result coverage, evaluation summaries, manuscript-style tables, and the method-claim check, then update docs and commit/push tracked summary/script changes only.
