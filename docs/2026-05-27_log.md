# 2026-05-27 Log

- Checked active Codex goal state: no thread-level goal was set at the start of the session. Set the active goal to complete the random-walk calibration gate workflow, confirm smoke/gate prerequisites, promote selected candidate parameter values, validate all default calibrations, start full simulations, and report scheduler/workspace status.
- Verified external Git metadata is still required because the workspace `.git` directory is not a normal writable checkout. Use `git --git-dir=/tmp/SharedQ.git --work-tree=/data/cheungyb/home/e1404425/SharedQ ...` for Git operations.
- Confirmed `qstat -u e1404425` initially showed no active jobs, meaning the 2026-05-24 calibration and gate-candidate jobs had finished.
- Ran `Rscript Simulation_random_walk/select_calibration_gate_candidate.R`; all four default settings reported `PASS`.
- Promoted selected gate candidates with `CALIBRATION_GATE_PROMOTE=1 Rscript Simulation_random_walk/select_calibration_gate_candidate.R`.
  - Setting I `balanced_small`: selected `gate_candidates/tol006`, max absolute validation error about `0.00685`.
  - Setting II `separated_moderate`: selected `gate_candidates/tol024`, max absolute validation error `0.024`.
  - Setting III `rw_sigma_moderate`: selected `gate_candidates/tol006`, max absolute validation error `0.006`.
  - Supplementary Setting III No Shared `separated_moderate`: selected `gate_candidates/tol024`, max absolute validation error `0.024`.
- Ran `VALIDATION_SPEC_MODE=default Rscript Simulation_random_walk/validate_candidate_calibration.R`; Setting I, Setting II, Setting III, and Supplementary Setting III No Shared all passed the default validation gate.
- Confirmed the May 19 Setting I calibration smoke report is stale relative to the promoted May 27 production artifact. The fresh selector and default validation reports are the active gate for production.
- Submitted full production simulations after the default gate passed:
  - Setting I: `516131.hn-10-03`
  - Setting II: `516132.hn-10-03`
  - Setting III: `516133.hn-10-03`
  - Supplementary Setting III No Shared: `516134.hn-10-03`
- `qstat -u e1404425` showed all four production jobs running on queue `short` with requested walltime `48:00`.
- Added `Simulation_random_walk/make_result_tables.R` to build manuscript-style tables after production completion. The script follows the existing `Writing/Tables and Figures.docx` and `Writing/Suppl Tables and Figures.docx` structure, checks that all result files are complete and newer than the promoted calibration artifact, writes main/supplemental tables under `Simulation_random_walk/Writing/generated_tables/`, and produces a method-claim comparison of SQ learning against Q learning and Q shared.
- Ran `TABLE_STATUS_ONLY=1 Rscript Simulation_random_walk/make_result_tables.R`. It correctly refused final table generation because production outputs are still incomplete/currently running: current May 27 result files exist only for `n=100`, while older `n=300`, `n=500`, and `n=1000` files are stale relative to the May 27 promoted calibration artifacts.
- Pre-push checkpoint at 2026-05-27 15:17 +08: all four full simulation jobs were still running. Refreshed `Simulation_random_walk/Writing/generated_tables/production_result_status.*`; current `n=100` non-null replicate counts were Setting I `54`, Setting II `42`, Setting III `66`, and Supplementary Setting III No Shared `110`, while later sample sizes were not yet refreshed under the promoted calibration.
- Next checkpoint: monitor those four jobs; after completion, run `Rscript Simulation_random_walk/summarize_setting_results.R`, then `Rscript Simulation_random_walk/make_result_tables.R`, inspect production result coverage, evaluation summaries, manuscript-style tables, and the method-claim check, then update docs and commit/push tracked summary/script changes only.
- Corrected the shared-parameter definition after user review: the shared relationship must be generated through true coefficients as `theta_j^(d) = mu_j + delta_j^(d)`, `delta_j^(d) ~ N(0, sigma_shared^2)`, not deterministic `mu + sigma`, `mu`, or `mu - sigma` offsets. `sigma_shared` is the standard deviation of the random true-coefficient deviation.
- Canceled the superseded full production simulation jobs submitted earlier today: `516131.hn-10-03`, `516132.hn-10-03`, `516133.hn-10-03`, and `516134.hn-10-03`. A follow-up `qstat -u e1404425` showed no active jobs.
- Updated Setting I and Setting III target builders so shared true coefficients are drawn from normal deviations around latent means using each spec seed. Corrected default draws are Setting I `balanced_small`: `Q3_A1=0.1102`, `Q2_A1=0.1366`, `Q3_A3=-0.3500`, `Q2_A2=-0.3500`, `Q3_A1A3=0.6033`, `Q2_A1A2=0.6121`; and Setting III `rw_sigma_moderate`: `Q3_A3=-0.3077`, `Q2_A2=-0.1997`, `Q1_A1=-0.2099`, `Q3_O3:A3=0.4630`, `Q2_O2:A2=0.5521`, `Q1_O1:A1=0.5803`, `Q3_A2:A3=0.3462`, `Q2_A1:A2=0.2504`.
- Updated validation and gate-selection tooling so a calibration artifact must match the current script-generated `theta_target`; this prevents old deterministic-offset artifacts from passing by validating only against their own stored target.
- Updated `docs/readme.md`, `docs/plan and goal.md`, and `RANDOM_WALK_SETTING_DESIGN_SUMMARY.md` with the corrected random shared true-parameter rule and marked the earlier Setting I/III promoted calibration values and canceled simulations as superseded.
- Ran `VALIDATION_SPEC_MODE=default Rscript Simulation_random_walk/validate_candidate_calibration.R`; Setting I and Setting III now correctly fail because their promoted artifacts store the old deterministic targets, while Setting II and Supplementary Setting III No Shared pass.
- Ran a bounded default smoke check with `CALIBRATION_MC_N=5000`, `CALIBRATION_MAXEVAL=50`, `CALIBRATION_LOCAL_MAXEVAL=20`, and `CALIBRATION_N_STARTS=1`. The corrected Setting I and Setting III smoke artifacts match the current random-shared `theta_target` exactly, but the low-budget optimizer does not reach the coefficient/difference gate; this is expected to require production-scale gate candidates.
- Submitted six replacement production-scale calibration gate candidates on queue `long`:
  - Setting I `balanced_small`, `rs_tol006`, target tolerance `0.006`: `516208.hn-10-03`
  - Setting I `balanced_small`, `rs_tol008`, target tolerance `0.008`: `516209.hn-10-03`
  - Setting I `balanced_small`, `rs_tol010`, target tolerance `0.010`: `516210.hn-10-03`
  - Setting III `rw_sigma_moderate`, `rs_tol006`, target tolerance `0.006`: `516211.hn-10-03`
  - Setting III `rw_sigma_moderate`, `rs_tol010`, target tolerance `0.010`: `516212.hn-10-03`
  - Setting III `rw_sigma_moderate`, `rs_tol008`, target tolerance `0.008`: `516213.hn-10-03`
- `qstat -u e1404425` showed all six replacement gate-candidate jobs in `R` state with requested walltime `72:00` and memory `16gb`.
- Refreshed `calibration_gate_candidate_selection` reports for Setting I and Setting III. They now correctly show `FAIL` for the old production/current and old `tol006`/`tol008` gate artifacts because their stored deterministic `theta_target` does not match the current random-shared script target. New `rs_*` artifacts will appear in the selection reports after the replacement jobs finish.
- Pushed correction checkpoint `2415785` (`Fix random shared simulation targets`) to `main`.
- Post-push scheduler checkpoint: `qstat -u e1404425` still shows all six replacement calibration gate candidates running on queue `long` with requested walltime `72:00` and memory `16gb`. Next action is to wait for these jobs, rerun gate selection for Setting I and Setting III, promote only passing corrected artifacts, and only then restart full simulations.
- Read the new root Project Quit / Forever Free Setting IV design file. It defines a parsimonious two-stage randomized design with main Setting IV default spec `pqff_shared_parsimonious` and supplementary no-shared default spec `pqff_separated_parsimonious`.
- Updated the Setting IV design file's sharing description to match the corrected project-wide random shared true-parameter rule: shared true coefficients are generated as `theta_j^(d) = mu_j + delta_j^(d)`, with `delta_j^(d) ~ N(0, sigma_shared^2)`, not deterministic `mu +/- sigma` offsets.
- Created implementation folders `Simulation_random_walk/Setting4/` and `Simulation_random_walk/SupplSetting4_NoShared/` with `Summarize/`, `calibration/`, `docs/`, `evaluation_results/`, `logs/`, `simulation_results/`, `test_results/`, `tests/`, and `working_log/` subfolders plus initial README, current-results, and todo files.
