# 2026-05-29 Log

Correction on 2026-06-02: the Setting IV fallback/source-data mode notes below are superseded. Accepted Setting IV production is synthetic-parametric only, with the cleaned PQ/FF data used only for structure and aggregate constants. See `docs/2026-06-02_log.md`.

- Checked production queue after the 2026-05-28 submissions. Setting II, Setting III, and Supplementary Setting III No Shared have completed; Setting I is still running as `523877.hn-10-03`.
- Current completed result coverage: Setting II, Setting III, and Supplementary Setting III No Shared each have `200` non-null replicates for `n = 100, 300, 500, 1000`.
- Setting I is still incomplete at `n = 1000`; do not include Setting I in manuscript tables until its job finishes and all expected results are complete.
- Started completed-job table generation work. Updated `Simulation_random_walk/make_result_tables.R` to support `TABLE_COMPLETED_ONLY=1`, so completed settings can be evaluated while Setting I is still running.
- Ran `TABLE_COMPLETED_ONLY=1 Rscript Simulation_random_walk/make_result_tables.R` after loading `r/4.4.0`.
  - Generated completed-setting outputs under `Simulation_random_walk/Writing/generated_tables/`.
  - Main manuscript-style completed table file: `completed_main_tables.md`. `Table 1` is marked pending because Setting I is still running; `Table 2` includes completed Setting II and Setting III results.
  - Supplement manuscript-style completed table file: `completed_supplement_tables.md`, including SQ-learning true/false positive summaries and Supplementary Setting III No Shared results.
  - Added Word outputs `completed_main_tables.docx` and `completed_supplement_tables.docx` because the requested reference formats are Word documents. Both generated `.docx` archives passed `unzip -t`.
  - Current method-claim summary from completed settings: `48` comparisons, `21` supported, support rate `0.4375`, `27` flagged.
- Added `SUMMARY_SETTINGS` filtering to `Simulation_random_walk/summarize_setting_results.R` and regenerated summaries only for completed settings with `SUMMARY_SETTINGS=Setting2,Setting3,SupplSetting3_NoShared`.
- Rechecked after Setting I finished. `qstat -u e1404425` showed no active jobs, and all four active settings had `200/200` non-null production replicates for `n = 100, 300, 500, 1000`.
- Ran full `Rscript Simulation_random_walk/summarize_setting_results.R` and full `Rscript Simulation_random_walk/make_result_tables.R`.
  - Regenerated all per-setting `Summarize/evaluation_summary.csv`, `current_results_summary.md`, and `todo.md` files.
  - Regenerated all table outputs under `Simulation_random_walk/Writing/generated_tables/`.
  - Added canonical Word outputs `reports_tables_and_figures_generated.docx` and `reports_suppl_tables_and_figures_generated.docx`, mirroring the requested reference report files. Both generated `.docx` archives passed `unzip -t`.
  - Final production status table shows every active setting/sample size as complete and current.
  - Final method-claim summary from all active settings: `64` comparisons, `25` supported, support rate `0.390625`, `39` flagged.
- Cleaned stale April setting-level logs from `docs/`:
  - `simulation_random_walk_setting1_2026-04-29_codex_log.md`
  - `simulation_random_walk_setting2_2026-04-29_codex_log.md`
  - `simulation_random_walk_setting3_2026-04-29_codex_log.md`
  - `simulation_random_walk_supplsetting3_noshared_2026-04-29_codex_log.md`
- Setting IV checkpoint:
  - Cleaned Project Quit / Forever Free source data is still absent, so accepted production Setting IV remains blocked.
  - Added executable main Setting IV scripts: `Q_datagenerating.R`, `Q_learning.R`, `Q_functions.R`, and `Simulation_Setting4.R`.
  - Production mode requires `SETTING4_SOURCE_DATA`. Synthetic fallback mode requires `SETTING4_ALLOW_SYNTHETIC=1`.
  - Ran a smoke test with `SETTING4_NS=30`, `SETTING4_N_REPS=2`, and `SETTING4_CALIBRATION_MC_N=1000`; output stayed local as `smoke_30_synthetic.rds`.
  - Started the requested synthetic fallback run for `n = 100, 300, 500, 1000`, `200` replicates each, and `SETTING4_CALIBRATION_MC_N=100000`. The user then asked to stop at a checkpoint; the active sandbox R process could not be interrupted through the session stdin, so the run was allowed to finish rather than leave a stray process.
  - Synthetic fallback results completed for all four sample sizes with `200/200` non-null replicates. These files are not accepted production results and must not be used in manuscript tables or method claims.
  - Updated `Simulation_random_walk/summarize_setting_results.R` so suffixed files such as `results_100_synthetic.rds` are classified as non-production and so vector sample-size parsing is correct.
  - Regenerated all setting summaries after fixing the parser. Setting IV now reports `0 of 4` production result files and all four expected synthetic fallback sample sizes as non-production outputs.
  - Verified no active `qstat` jobs and no matching local `Simulation_Setting4`/random-walk Rscript process before staging this checkpoint.
