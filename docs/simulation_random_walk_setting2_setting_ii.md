### Settings

In this section, we propose three simulation designs to evaluate the performance of our Q learning model with three goals: 

- I. Show that our method can exhibit less bias for those shared parameters. Even when mis-specified, the bias generated by fused ridge can be tolerated.
- II. Show that our method can serve as a more flexible choice compared to conventional Q learning and Q learning with mixed effects, in terms of less biased estimate, better assignment and more flexibility.
- III. Show that our method is not biased when there is no shared parameter.

For each setting, we consider a sample size 100, 300, 500 and 1000. 

#### Setting II - A mimic of the real data in our study, but without any shared parameter

Linear model, shared covariates and shared confounder at last two stage, 3 stage:

##### Primary Outcome

$$
\begin{aligned}
Y_{primary} &= \gamma_{1}+\gamma_{2}A_{1} +
\\&+(1-R_1)\gamma_{3}+\gamma_{4}A_2+\gamma_{5}A_1A_2
\\&+(1-R_2)\gamma_{6}+\gamma_{7}A_3 + \gamma_{8}A_1A_3+\gamma_{9}A_2A_3+
\\&+\epsilon_1
\end{aligned}
$$

where $\boldsymbol\gamma$ denotes the true parameters for generating primary outcomes at each stage. In the real data analysis, we only observe the initial outcome and final outcome at week 4. Thus, in the simulation, we only simulate the final primary outcome.

$A_1, A_2, A_3$ are treatments assigned at each stage, where $P[A_1 = 1] = P[A_1 = -1] = \frac{1}{2}$, $P[A_2 =1|R_1 = 0]=P[A_2 =-1|R_1 = 0] = \frac{1}{2}$ and $P[A_3 =1|R_2 = 0]=P[A_3 =-1|R_2 = 0] = \frac{1}{2}$.  Responders ($R_i = 1$) at stage $i$ will be assigned to $A_{j+1} = -1$ at next stage.



In this setting, responders are included in the study but are not randomized. Instead, we construct a binary variable to indicate responders. Therefore, the treatment effect should be estimated only within non-responders only. The probabilities of responders can be defined in two ways:

- A fixed number:

$$
\begin{aligned}
P(R_1 = 1) = 0.59\\

P(R_2=1 | A_1 = 1, A_2=1) = 0.23\\
P(R_2=1 | A_1 = 1, A_2=-1) = 0.23\\
P(R_2=1 | A_1 = -1, A_2=1) = 0.13\\
P(R_2=1 | A_1 = -1, A_2=-1) = 0.13
\end{aligned}
$$



- Determined by $\boldsymbol A$. (Not Used)
  $$
  \begin{aligned}
  P(R_1 = 1) = \text{logit}\{\delta_{10}+\delta_{11}A_1 \}\\
  
  P(R_2=1) = \text{logit}\{\delta_{20}+\delta_{21}A_1 +\delta_{22}A_2\}
  \end{aligned}
  $$

##### Q-models

##### Primary Outcome

$$
\begin{aligned}
Q_3 &= \beta_{30}+\beta_{31}A_1 +\beta_{32}A_2 + \beta_{33}A_1A_2+\beta_{34}(1-R_1)+A_3[\beta_{35}+\beta_{36}A_1+\beta_{37}A_2]\\
Q_2 &= \beta_{20}+\beta_{21}A_1 +A_2[\beta_{22}+ \beta_{23}A_1]\\
Q_1 &= \beta_{10} + \beta_{11}A_1\\
\end{aligned}
$$

For stage 2 and 3, we only use data for non-responders.

For unshared scenario, we do not use mis-specified models, as all shared models are inherently mis-specified. When running simulations, you may use the old shared pattern as the misspecification case, you may propose other misspecification cases as well. But do note that you need to write a markdown to record the misspecification variable you specified, and please do remember to try multiple groups of parameter choices. For how to derive the true parameters, see MCMC below.



#### MCMC for Simulation data generating

Instead of knowing the exact parameter for data generating, we want to directly specify the vector $\boldsymbol \psi$ for Q learning.

- First, specify the vector $\boldsymbol \psi$. 

- Let $\boldsymbol \gamma$ be the parameters used to generate primary outcomes for each stage. Start with a random $\boldsymbol \gamma$ and generate a large Monte Carlo dataset without random errors. In this case, we expect to use this large dataset to represent the true Q values we should have (the sum between average outcomes from responders and max(Q) value of next stage from non-responders).

- With these "true" Q values, we inversely calculate the $\boldsymbol \psi$ from our proposed model structure (run regressions). Of course the $\hat {\boldsymbol \psi}$ is not we specified in the first step. Now we can construct an objective function based on this:

  - $$
    L(\boldsymbol \gamma) = ||\hat {\boldsymbol \psi} - \boldsymbol \psi||^2_2
    $$

- Now the question becomes find $\boldsymbol \gamma$ that minimize the objective function. To ensure the identifiability, we put a constraint $||\gamma||_2^2 = 1$ or some constraints on the domain of $\boldsymbol \gamma$ for a narrower searching area.

IMPORTANT: When running nloptr to obtain the true estimates, do remember to adjust the search interval by adjusting the constraints within the code files to obtain the values desired. After obtaining true estimates, double check the shared parameter to see if it really shares (exhibits similar values in you specification) like what you expect.







